import 'package:flutter/material.dart';

/// Professional color palette for the ERP application
/// Following modern design system principles with semantic color naming
class AppColors {
  // Primary Brand Colors
  static const primaryBlue = Color(0xFF1E40AF);     // More professional, deeper blue
  static const primaryBlueLight = Color(0xFF3B82F6); // Lighter variant
  static const primaryBlueDark = Color(0xFF1E3A8A);  // Darker variant

  // Neutral Colors
  static const surfaceLight = Color(0xFFFAFAFA);     // Softer background
  static const surfaceCard = Color(0xFFFFFFFF);      // Card background
  static const surfaceDark = Color(0xFFF5F5F5);      // Darker surface

  // Text Colors
  static const textPrimary = Color(0xFF111827);      // Primary text
  static const textSecondary = Color(0xFF6B7280);    // Secondary text
  static const textTertiary = Color(0xFF9CA3AF);     // Tertiary text

  // Status Colors
  static const success = Color(0xFF059669);          // Success states
  static const successLight = Color(0xFFF0FDF4);     // Success background
  static const warning = Color(0xFFD97706);          // Warning states
  static const warningLight = Color(0xFFFFFBEB);     // Warning background
  static const error = Color(0xFFDC2626);            // Error states
  static const errorLight = Color(0xFFFEF2F2);       // Error background
  static const info = Color(0xFF2563EB);             // Info states
  static const infoLight = Color(0xFFEFF6FF);        // Info background

  // Border Colors
  static const borderLight = Color(0xFFE5E7EB);      // Light borders
  static const borderMedium = Color(0xFFD1D5DB);     // Medium borders
  static const borderDark = Color(0xFF9CA3AF);       // Dark borders

  // Chip and Badge Colors
  static const chipGrey = Color(0xFFF3F4F6);         // Neutral chip background
  static const chipGreyText = Color(0xFF374151);     // Neutral chip text

  // Gradient Colors
  static const gradientStart = Color(0xFF3B82F6);
  static const gradientEnd = Color(0xFF1E40AF);

  // Shadow Colors
  static const shadowLight = Color(0x0F000000);      // Light shadow
  static const shadowMedium = Color(0x1F000000);     // Medium shadow
  static const shadowDark = Color(0x25000000);       // Dark shadow
}
